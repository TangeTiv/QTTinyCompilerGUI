# TINY 编译器 — 测试结果汇报

## 测试环境

- **操作系统**: Windows 11
- **开发环境**: Qt 6.9.1 + MinGW 64-bit
- **测试方式**: 在 GUI 界面中打开测试文件，点击"生成语法树"按钮

---

## 测试用例 1：基本输入输出

**文件**: `test1_basic.tny`

**源代码**:
```
read x;
read y;
z := x + y;
write z
```

**预期结果**: 语法分析成功，语法树包含 Read、Read、Assign、Write 节点

**实际结果**: 

![image-20260511203806697](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511203806697.png)

---

## 测试用例 2：条件语句 (if-then-else)

**文件**: `test2_if.tny`

**源代码**:
```
read a;
if a > 0 then
  write 1
else
  write 0
end
```

**预期结果**: 语法分析成功，语法树包含 If 节点及其分支

**实际结果**: 

![image-20260511203956235](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511203956235.png)

---

## 测试用例 3：while 循环

**文件**: `test3_while.tny`

**源代码**:
```
{ Test while loop }
x := 0;
while (x <= 5) 
  write x;
  x := x + 1
endwhile

```

**预期结果**: 语法分析成功，语法树包含 While 节点及循环体

**实际结果**: 

![image-20260511204116717](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511204116717.png)

---

## 测试用例 4：for 循环

**文件**: `test4_for.tny`

**源代码**:
```
for(i := 0; i < 10; ++i)
  write i
```

**预期结果**: 语法分析成功，语法树包含 For 节点及自增操作

**实际结果**: 

![image-20260511204213919](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511204213919.png)

---

## 测试用例 5：扩展运算符

**文件**: `test5_extended.tny`

**源代码**:
```
read x;
read y;
mod := x % 3;
pow := mod ^ 2;
if x <> 0 then
  write pow
end
```

**预期结果**: 语法分析成功，支持 %、^、<> 等扩展运算符

**实际结果**: 

![image-20260511204234096](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511204234096.png)

---

## 测试用例 6：综合测试

**文件**: `test_all_features.tny`

**覆盖特性**:
- for 循环与自减 `--count`
- while 循环与自增 `++x`
- 取模 `%`、幂运算 `^`
- 正则赋值 `::=` 及 `|`、`&`、`#` 操作
- 不等号 `<>` 比较
- 嵌套 if 语句

**实际结果**: 

![image-20260511204315061](C:\Users\谭\AppData\Roaming\Typora\typora-user-images\image-20260511204315061.png)

---

## 测试结论

所有测试用例均通过，编译器各功能模块正常工作：
| 功能 | 状态 |
|------|------|
| 基本输入输出 (read/write) | 通过 |
| 算术运算 (+, -, *, /) | 通过 |
| if-then-else 条件语句 | 通过 |
| while 循环 | 通过 |
| for 循环 | 通过 |
| 扩展运算符 (%, ^, ++, --) | 通过 |
| 扩展比较 (<=, >=, <>) | 通过 |
| 正则表达式赋值 (::=) | 通过 |
| 语法树图形化显示 | 通过 |
| 错误检测与报告 | 通过 |
